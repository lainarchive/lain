[lain]
name = "lain."
version = "0.1.0"

[models]
backend = "llama.cpp"
model = "C:\\Users\\User\\.ollama\\models\\blobs\\sha256-02992039bd35f0c89d90cef147cdcb2b9a741d8b4b585bef8331c33679de1e3a"

[agents]
count = 8

[paths]
# Discovery roots are configurable through LAIN_PROJECT_ROOTS.
# The default is the user's home directory; only recognizable projects
# directly inside the root are recorded.
projects = "~"
memory = "~/lain/memory"
sandbox = "~/lain/sandbox"
